name: ci

on:
  push:
    branches: [main]
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.12", "3.14"]
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          python-version: ${{ matrix.python-version }}
      - run: uv sync --locked
      - run: make lint
      - run: make types
      - run: make test
      # Determinism canary: selftest digest must be stable (also asserted in pytest).
      - run: make selftest
      # No-drift gate: committed generated manuals must match content (ADR-0003 §4).
      - run: make generate-check

  # Reserved future stages, per docs/engineering/testing.md:
  #   perf-budgets (M2, pinned runner)
